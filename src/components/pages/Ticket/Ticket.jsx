import { Layout } from '../../App/Layout/Layout'
import { Form } from '../../Partials/Form/Form'

export const Ticket = () => {
    return(
        <Layout>
            <Form />
        </Layout>
    )
}